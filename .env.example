CLAPBOARD_API_BASE_URL=
CLAPBOARD_API_TOKEN=
CLAPBOARD_API_TIMEOUT_MS=5000
# 16文字以上のランダム文字列。本番では必ず設定すること。未設定だと
# /, /projects, /code-review と /api/* (health/login/logout を除く) は 503 を返す。
# 推奨: `openssl rand -base64 32` で生成（44文字、固定長エントロピー高め）
CLAPBOARD_ACCESS_TOKEN=
